_meta:
  name: max_stack
  created_at: '2026-05-01T18:25:26.810900+00:00'
  source: thesis
  status: paper
  notes: "max_open_legs=20, max_equity_per_leg=0.04 \u2192 up to 20 simultaneous positions\
    \ at ~4% equity each. Hypothesis: very high margin utilisation will trigger collateral\
    \ / free-margin checks; logs will show exactly where the bot stops opening new\
    \ legs."
  goal: null
  fitness: null
strategy:
  expiry_execution_hour: 8
  expiry_preference:
  - weekly
  - monthly
  initial_cycle: put
  iv_dynamic_delta: true
  iv_rank_threshold: 0.3
  liquidity_top_n: 100
  max_dte: 37
  min_dte: 3
  target_delta_max: 0.4
  target_delta_min: 0.18
sizing:
  collateral_buffer: 1.5
  contract_size_btc: 0.1
  ladder_enabled: false
  ladder_legs: 2
  max_equity_per_leg: 0.04
  max_open_legs: 20
  min_free_equity_fraction: 0.02
  regime_ma_days: 50
  use_regime_filter: false
risk:
  kill_switch_file: KILL_SWITCH
  max_adverse_delta: 0.4
  max_daily_drawdown: 0.1
  max_loss_per_leg: 0.02
  roll_enabled: false
  roll_min_dte: 3
hedge:
  enabled: true
  rebalance_threshold: 0.05
deribit:
  currency: BTC
  request_timeout: 10
  rest_url_mainnet: https://www.deribit.com/api/v2
  rest_url_testnet: https://test.deribit.com/api/v2
  testnet: true
  ws_url_mainnet: wss://www.deribit.com/ws/api/v2
  ws_url_testnet: wss://test.deribit.com/ws/api/v2
execution:
  order_confirm_timeout: 30
  poll_interval: 60
  slippage_tolerance: 0.005
logging:
  level: INFO
  log_dir: logs
  retention: 30 days
  rotation: 1 day
  trade_log_csv: data/trades.csv
overseer:
  check_interval_minutes: 60
  drawdown_warning_threshold: 0.05
  enabled: true
  iv_spike_warning_threshold: 0.85
backtest:
  approx_otm_offset: 0.0338
  lookback_months: 12
  premium_fraction_of_spot: 0.0289
  results_csv: data/backtest_trades.csv
  results_image: backtest_results.png
  risk_free_rate: 0.04
  starting_equity: 100000.0
  transaction_cost: 0.5
